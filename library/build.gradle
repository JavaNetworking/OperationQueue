apply plugin: 'java'

defaultTasks 'clean'

repositories {
    mavenCentral()
    maven {
        url "https://repo1.maven.org/maven2"
    }
}

dependencies {
    testCompile group: 'junit', name: 'junit', version: '4.11'
}

jar {
    baseName = 'OperationQueue'
    version =  '0.3.1'
}
